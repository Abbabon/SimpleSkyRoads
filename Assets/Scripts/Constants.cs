/// <summary>
/// Names of the action maps and actions inside Assets/InputSystem_Actions.inputactions.
/// The Input System looks actions up by string, so keeping the strings in one place means a
/// rename in the .inputactions asset only has to be mirrored here.
/// </summary>
public static class Constants
{
    public const string PlayerActionMap = "Player";
    public const string SteerAction = "Steer";
    public const string BoostAction = "Boost";
    public const string StartAction = "Start";
}
